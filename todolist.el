
(defvar todo-file (expand-file-name "todolist.org" user-emacs-directory))

(defvar todo-file-archive (expand-file-name "todolist-archive.org" user-emacs-directory))

(defun todolist ()
  (interactive)
  ())

(provide 'todolist)
;;; todolist.el ends here
